# Production build settings — used automatically by `npm run build`.
# Safe to commit: these are public URLs, not secrets.

# API + uploaded images are served from the API subdomain.
REACT_APP_BASE_URL=https://api.fashionfreude.com

# Storefront origin, used to build the URLs in public/sitemap.xml.
SITE_URL=https://fashionfreude.com
